<% if cart.cart_items.present? %>
  <ul class="list-group">
    <% cart.cart_items.each do |cart_item| %>
      <li class="list-group-item"><%= cart_item.item.name %></li>
    <% end %>
  </ul>
<% else %>
  <p class="text-center">Vous n'avez encore rien ajouté à votre panier</p>
<% end %>
