<h2 class="mt-4 py-5 text-center bg-primary text-warning">La Boutique des Nouveaux Designs</h2>

<div class="mt-4">
  <%= render 'carrousel' %>
</div>

<div class="d-flex flex-wrap justify-content-center">
  <% @items.each do |item| %>
    <%= render 'items/item_card', item: item %>
  <% end %>
</div>

<div class="actions m-5 d-flex justify-content-center">
  <%= link_to "Comment devenir créateur ?", become_maker_path, :class=>"btn btn-success" %>
</div>
