<div class="container">
  <h2 class="pt-4 text-center text-primary"><%= @category.name %></h2>

  <div class="row row-cols">
    <% @items.each do |item| %>
      <% if item.available_qty > 0 %>
        <%= render 'items/item_card', item: item %>
      <% end %>
    <% end %>
  </div>
</div>
