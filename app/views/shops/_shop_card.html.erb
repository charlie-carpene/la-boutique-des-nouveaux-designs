<div class="card bg-light text-dark">
  <%= image_tag shop.show_image(shop), class: "card-img" %>
  <% unless shop.image.present? %>
    <div class="card-img-overlay d-flex justify-content-center mt-5 pt-5">
      <p class="card-text text-warning font-weight-bold">Nouveau<br> créateur</p>
    </div>
  <% end %>
  <div class="card-img-overlay d-flex flex-column align-items-center justify-content-end">
    <%= button_to shop.brand, shop_path(shop.id), {method: 'get', form_class: "m-2 pt-1", class: "btn btn-primary" } %>
  </div>
</div>
