<div class="container">
  <h1><%= @user.email %></h1>

  <%= button_to "Accepter", user_path(@user.id), method: 'put', :class => "btn btn-success btn-lg btn-block" %>
  <%= button_to "Refuser", shop_path(@user.shop.id), method: 'delete', :class => "btn btn-danger btn-lg btn-block" %>
</div>
